import { makeStyles } from "@material-ui/core/styles";

const GridTechnicalImages = makeStyles({
  image: {
    objectFit: "contain",
    width: "100%",
    height: "9vh",
  },
});

export default GridTechnicalImages;
