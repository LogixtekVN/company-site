import { makeStyles } from "@material-ui/core/styles";

const CustomImage = makeStyles({
  image: {
    height: "22vh",
    width: "100%",
    objectFit: "contain",
  },
});

export default CustomImage;
