import { makeStyles } from "@material-ui/core/styles";

const IntroductionBox = makeStyles({
  introBox: {
    margin: "48px 12%",
    fontSize: 18,
    textAlign: "center",
  },
});

export default IntroductionBox;
